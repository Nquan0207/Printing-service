# Legacy apparel catalog

This directory preserves the repository's previous SQLAlchemy crawler and its
tests for reference. It is intentionally excluded from the active test suite
and is not maintained as a runnable package.

The active application uses `stockroom_crawler/`, `go-backend/`, and
`app/mcp_server/` from the repository root.
